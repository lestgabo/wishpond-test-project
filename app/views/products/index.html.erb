<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Pictures</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= @products.current_path %>
    <% @products.each do |products| %>
      <tr>
        <td> <%= products.name %></td>
        <td> <%=  %> </td>
        <td> <%=  %> </td>
        <td> <%#= image_tag product.pictures %></td>
        <td><%#= product.pictures %></td>
        <td><%= link_to 'Show', products %></td>
        <td><%= link_to 'Edit', edit_product_path(products) %></td>
        <td><%= link_to 'Destroy', products, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
