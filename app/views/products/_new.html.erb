
<%= form_for @product, html: { multipart: true} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.label :pictures %>
  <%= f.file_field :pictures, multiple: true %>
  
  <%= f.submit "Submit" %>
<% end %>